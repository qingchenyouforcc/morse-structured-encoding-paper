[project]
name = "morse-structured-encoding-paper"
version = "0.1.0"
description = "Paper-aligned experiments for reversible structured encoding of Morse sequences."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "zstandard>=0.25.0",
]

[tool.uv]
package = false
